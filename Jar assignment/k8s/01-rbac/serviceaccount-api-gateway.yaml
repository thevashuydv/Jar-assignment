# Dedicated ServiceAccount for api-gateway (do not use default).
# Apply with: kubectl apply -f k8s/01-rbac/ -n <namespace>
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  # namespace set at apply time via -n staging or -n production
